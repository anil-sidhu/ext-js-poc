Ext.define('StudentApp.controller.CustomerController', {
    extend: 'Ext.app.Controller',
    models: [],
    views: ['StudentApp.view.Customers'],
    init: function () {
        // var studentStore = Ext.getStore('StudentApp.store.Customers');
        // studentStore.load();
    }    
});
